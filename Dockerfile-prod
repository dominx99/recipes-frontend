FROM node:19

COPY --from=node:19 /usr/local/bin/node /usr/local/sbin/node

WORKDIR /application

COPY . .

RUN npm ci --omit=dev
RUN npm run build
RUN npm install -g serve

ENV PATH /application/node_modules/.bin:$PATH
